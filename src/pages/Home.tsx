import Layout from "../layout/Layout";

// Custom Components
import SuperEvent from "../components/SuperEvent";

const Home = () => {
  return (
    <Layout>
      <SuperEvent />
    </Layout>
  );
};

export default Home;
